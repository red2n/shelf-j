# The 12 business services. Each: own Postgres role/schema, registers with Consul under
# STOREQL_ADVERTISE_HOST (must equal this file's k8s Service name — the gateway and any
# peer service resolve each other via Consul's stored Address, not k8s DNS directly).
#
# Probes use the full 3-endpoint model (ARCHITECTURE.md DoD): /health/started gates how
# long the JVM + inline Flyway migration get to boot before being killed; /health/ready
# gates traffic (checks real DB/Kafka/config reachability); /health/live triggers a
# restart. docker-compose.yml's single Docker healthcheck only used /health/live for all
# three purposes — Kubernetes lets us use the granular endpoints each service already
# exposes.
#
# Flyway migrations run inline at boot (FlywayRunner, shared/common-service) — there is
# no standalone "migrate-only" entry point in this codebase today, so this mirrors
# exactly how docker-compose.yml and the storeql.com VPS deployment already run in
# production. Safe at replicas: 1 (no concurrent-migration race); re-check before ever
# scaling any of these to >1 replica.

---
apiVersion: v1
kind: Service
metadata: { name: iam-svc, namespace: storeql }
spec:
  selector: { app: iam-svc }
  ports: [{ port: 8080, targetPort: 8080 }]
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: iam-svc, namespace: storeql }
spec:
  replicas: 1
  selector: { matchLabels: { app: iam-svc } }
  template:
    metadata: { labels: { app: iam-svc } }
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - { name: tmp, emptyDir: {} }
      containers:
        - name: iam-svc
          image: ghcr.io/red2n/storeql-iam-svc:latest
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
          ports: [{ containerPort: 8080 }]
          envFrom:
            - configMapRef: { name: storeql-common-env }
            - secretRef: { name: storeql-config-token }
            # key "storeql.jwt.secret" seals the token signing keys' private halves at rest (20.15);
            # tokens themselves are RS256 under a rotating key. Only iam-svc is given it.
            - secretRef: { name: storeql-jwt }
          env:
            - { name: STOREQL_DB_USER, value: "iam_svc" }
            - name: STOREQL_DB_PASSWORD
              valueFrom: { secretKeyRef: { name: storeql-postgres, key: IAM_DB_PASSWORD } }
            - { name: JAVA_TOOL_OPTIONS, value: "-Xms128m -Xmx512m -XX:+UseG1GC" }
            - { name: STOREQL_ADVERTISE_HOST, value: "iam-svc" }
            # Single sign-on (20.x): set both to this deployment's own hosts to offer it. The
            # callback is iam-svc's as a browser reaches it through the gateway, and is what a
            # business registers with its identity provider; unset, single sign-on is unavailable.
            # storeql.sso.insecure-hosts stays unset here: a provider is HTTPS on the internet.
            # - { name: STOREQL_SSO_CALLBACK_URL, value: "https://api.<your-domain>/api/iam-svc/auth/sso/callback" }
            # - { name: STOREQL_SSO_RETURN_ORIGINS, value: "https://app.<your-domain>" }
          startupProbe:
            httpGet: { path: /health/started, port: 8080 }
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /health/ready, port: 8080 }
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet: { path: /health/live, port: 8080 }
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            requests: { cpu: 200m, memory: 450Mi }
            limits: { cpu: 500m, memory: 700Mi }

---
apiVersion: v1
kind: Service
metadata: { name: tenant-svc, namespace: storeql }
spec:
  selector: { app: tenant-svc }
  ports: [{ port: 8080, targetPort: 8080 }]
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: tenant-svc, namespace: storeql }
spec:
  replicas: 1
  selector: { matchLabels: { app: tenant-svc } }
  template:
    metadata: { labels: { app: tenant-svc } }
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - { name: tmp, emptyDir: {} }
      containers:
        - name: tenant-svc
          image: ghcr.io/red2n/storeql-tenant-svc:latest
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
          ports: [{ containerPort: 8080 }]
          envFrom:
            - configMapRef: { name: storeql-common-env }
            - secretRef: { name: storeql-config-token }
          env:
            - { name: STOREQL_DB_USER, value: "tenant_svc" }
            - name: STOREQL_DB_PASSWORD
              valueFrom: { secretKeyRef: { name: storeql-postgres, key: TENANT_DB_PASSWORD } }
            - { name: JAVA_TOOL_OPTIONS, value: "-Xms128m -Xmx512m -XX:+UseG1GC" }
            - { name: STOREQL_ADVERTISE_HOST, value: "tenant-svc" }
          startupProbe:
            httpGet: { path: /health/started, port: 8080 }
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /health/ready, port: 8080 }
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet: { path: /health/live, port: 8080 }
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            requests: { cpu: 200m, memory: 450Mi }
            limits: { cpu: 500m, memory: 700Mi }

---
apiVersion: v1
kind: Service
metadata: { name: inventory-svc, namespace: storeql }
spec:
  selector: { app: inventory-svc }
  ports: [{ port: 8080, targetPort: 8080 }]
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: inventory-svc, namespace: storeql }
spec:
  replicas: 1
  selector: { matchLabels: { app: inventory-svc } }
  template:
    metadata: { labels: { app: inventory-svc } }
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - { name: tmp, emptyDir: {} }
      containers:
        - name: inventory-svc
          image: ghcr.io/red2n/storeql-inventory-svc:latest
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
          ports: [{ containerPort: 8080 }]
          envFrom:
            - configMapRef: { name: storeql-common-env }
            - secretRef: { name: storeql-config-token }
          env:
            - { name: STOREQL_DB_USER, value: "inventory_svc" }
            - name: STOREQL_DB_PASSWORD
              valueFrom: { secretKeyRef: { name: storeql-postgres, key: INVENTORY_DB_PASSWORD } }
            - { name: JAVA_TOOL_OPTIONS, value: "-Xms128m -Xmx512m -XX:+UseG1GC" }
            - { name: STOREQL_ADVERTISE_HOST, value: "inventory-svc" }
          startupProbe:
            httpGet: { path: /health/started, port: 8080 }
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /health/ready, port: 8080 }
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet: { path: /health/live, port: 8080 }
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            requests: { cpu: 200m, memory: 450Mi }
            limits: { cpu: 500m, memory: 700Mi }

---
apiVersion: v1
kind: Service
metadata: { name: order-svc, namespace: storeql }
spec:
  selector: { app: order-svc }
  ports: [{ port: 8080, targetPort: 8080 }]
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: order-svc, namespace: storeql }
spec:
  replicas: 1
  selector: { matchLabels: { app: order-svc } }
  template:
    metadata: { labels: { app: order-svc } }
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - { name: tmp, emptyDir: {} }
      containers:
        - name: order-svc
          image: ghcr.io/red2n/storeql-order-svc:latest
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
          ports: [{ containerPort: 8080 }]
          envFrom:
            - configMapRef: { name: storeql-common-env }
            - secretRef: { name: storeql-config-token }
          env:
            - { name: STOREQL_DB_USER, value: "order_svc" }
            - name: STOREQL_DB_PASSWORD
              valueFrom: { secretKeyRef: { name: storeql-postgres, key: ORDER_DB_PASSWORD } }
            - { name: JAVA_TOOL_OPTIONS, value: "-Xms128m -Xmx512m -XX:+UseG1GC" }
            - { name: STOREQL_ADVERTISE_HOST, value: "order-svc" }
            # docs/vps-deployment.md §5: "Server-side pricing must be true in production."
            - { name: STOREQL_ORDER_PRICING_ENFORCE, value: "true" }
            # Stock holds for ONLINE checkout. Left false (the dev default) until you've
            # seeded real inventory for your tenants — flip to "true" once you have, or
            # every online checkout will 409 on stock. See docker-compose.yml's comment
            # on storeql.order.inventory.reserve-enforce.
            - { name: STOREQL_ORDER_RESERVE_ENFORCE, value: "false" }
          startupProbe:
            httpGet: { path: /health/started, port: 8080 }
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /health/ready, port: 8080 }
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet: { path: /health/live, port: 8080 }
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            requests: { cpu: 200m, memory: 450Mi }
            limits: { cpu: 500m, memory: 700Mi }

---
apiVersion: v1
kind: Service
metadata: { name: purchase-svc, namespace: storeql }
spec:
  selector: { app: purchase-svc }
  ports: [{ port: 8080, targetPort: 8080 }]
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: purchase-svc, namespace: storeql }
spec:
  replicas: 1
  selector: { matchLabels: { app: purchase-svc } }
  template:
    metadata: { labels: { app: purchase-svc } }
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - { name: tmp, emptyDir: {} }
      containers:
        - name: purchase-svc
          image: ghcr.io/red2n/storeql-purchase-svc:latest
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
          ports: [{ containerPort: 8080 }]
          envFrom:
            - configMapRef: { name: storeql-common-env }
            - secretRef: { name: storeql-config-token }
          env:
            - { name: STOREQL_DB_USER, value: "purchase_svc" }
            - name: STOREQL_DB_PASSWORD
              valueFrom: { secretKeyRef: { name: storeql-postgres, key: PURCHASE_DB_PASSWORD } }
            - { name: JAVA_TOOL_OPTIONS, value: "-Xms128m -Xmx384m -XX:+UseG1GC" }
            - { name: STOREQL_ADVERTISE_HOST, value: "purchase-svc" }
          startupProbe:
            httpGet: { path: /health/started, port: 8080 }
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /health/ready, port: 8080 }
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet: { path: /health/live, port: 8080 }
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            requests: { cpu: 150m, memory: 320Mi }
            limits: { cpu: 400m, memory: 512Mi }

---
apiVersion: v1
kind: Service
metadata: { name: cart-svc, namespace: storeql }
spec:
  selector: { app: cart-svc }
  ports: [{ port: 8080, targetPort: 8080 }]
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: cart-svc, namespace: storeql }
spec:
  replicas: 1
  selector: { matchLabels: { app: cart-svc } }
  template:
    metadata: { labels: { app: cart-svc } }
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - { name: tmp, emptyDir: {} }
      containers:
        - name: cart-svc
          image: ghcr.io/red2n/storeql-cart-svc:latest
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
          ports: [{ containerPort: 8080 }]
          envFrom:
            - configMapRef: { name: storeql-common-env }
            - secretRef: { name: storeql-config-token }
          env:
            - { name: STOREQL_DB_USER, value: "cart_svc" }
            - name: STOREQL_DB_PASSWORD
              valueFrom: { secretKeyRef: { name: storeql-postgres, key: CART_DB_PASSWORD } }
            - { name: JAVA_TOOL_OPTIONS, value: "-Xms128m -Xmx384m -XX:+UseG1GC" }
            - { name: STOREQL_ADVERTISE_HOST, value: "cart-svc" }
            # Backs the cart/cart-items cache-aside reads (CartRepository).
            - name: STOREQL_REDIS_PASSWORD
              valueFrom: { secretKeyRef: { name: storeql-redis, key: REDIS_PASSWORD } }
          startupProbe:
            httpGet: { path: /health/started, port: 8080 }
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /health/ready, port: 8080 }
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet: { path: /health/live, port: 8080 }
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            requests: { cpu: 150m, memory: 320Mi }
            limits: { cpu: 400m, memory: 512Mi }

---
apiVersion: v1
kind: Service
metadata: { name: pricing-svc, namespace: storeql }
spec:
  selector: { app: pricing-svc }
  ports: [{ port: 8080, targetPort: 8080 }]
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: pricing-svc, namespace: storeql }
spec:
  replicas: 1
  selector: { matchLabels: { app: pricing-svc } }
  template:
    metadata: { labels: { app: pricing-svc } }
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - { name: tmp, emptyDir: {} }
      containers:
        - name: pricing-svc
          image: ghcr.io/red2n/storeql-pricing-svc:latest
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
          ports: [{ containerPort: 8080 }]
          envFrom:
            - configMapRef: { name: storeql-common-env }
            - secretRef: { name: storeql-config-token }
          env:
            - { name: STOREQL_DB_USER, value: "pricing_svc" }
            - name: STOREQL_DB_PASSWORD
              valueFrom: { secretKeyRef: { name: storeql-postgres, key: PRICING_DB_PASSWORD } }
            - { name: JAVA_TOOL_OPTIONS, value: "-Xms128m -Xmx384m -XX:+UseG1GC" }
            - { name: STOREQL_ADVERTISE_HOST, value: "pricing-svc" }
          startupProbe:
            httpGet: { path: /health/started, port: 8080 }
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /health/ready, port: 8080 }
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet: { path: /health/live, port: 8080 }
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            requests: { cpu: 150m, memory: 320Mi }
            limits: { cpu: 400m, memory: 512Mi }

---
apiVersion: v1
kind: Service
metadata: { name: payment-svc, namespace: storeql }
spec:
  selector: { app: payment-svc }
  ports: [{ port: 8080, targetPort: 8080 }]
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: payment-svc, namespace: storeql }
spec:
  replicas: 1
  selector: { matchLabels: { app: payment-svc } }
  template:
    metadata: { labels: { app: payment-svc } }
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - { name: tmp, emptyDir: {} }
      containers:
        - name: payment-svc
          image: ghcr.io/red2n/storeql-payment-svc:latest
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
          ports: [{ containerPort: 8080 }]
          envFrom:
            - configMapRef: { name: storeql-common-env }
            - secretRef: { name: storeql-config-token }
          env:
            - { name: STOREQL_DB_USER, value: "payment_svc" }
            - name: STOREQL_DB_PASSWORD
              valueFrom: { secretKeyRef: { name: storeql-postgres, key: PAYMENT_DB_PASSWORD } }
            - { name: JAVA_TOOL_OPTIONS, value: "-Xms128m -Xmx256m -XX:+UseG1GC" }
            - { name: STOREQL_ADVERTISE_HOST, value: "payment-svc" }
          startupProbe:
            httpGet: { path: /health/started, port: 8080 }
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /health/ready, port: 8080 }
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet: { path: /health/live, port: 8080 }
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            requests: { cpu: 100m, memory: 220Mi }
            limits: { cpu: 300m, memory: 384Mi }

---
apiVersion: v1
kind: Service
metadata: { name: product-svc, namespace: storeql }
spec:
  selector: { app: product-svc }
  ports: [{ port: 8080, targetPort: 8080 }]
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: product-svc, namespace: storeql }
spec:
  replicas: 1
  selector: { matchLabels: { app: product-svc } }
  template:
    metadata: { labels: { app: product-svc } }
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - { name: tmp, emptyDir: {} }
      containers:
        - name: product-svc
          image: ghcr.io/red2n/storeql-product-svc:latest
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
          ports: [{ containerPort: 8080 }]
          envFrom:
            - configMapRef: { name: storeql-common-env }
            - secretRef: { name: storeql-config-token }
          env:
            - { name: STOREQL_DB_USER, value: "product_svc" }
            - name: STOREQL_DB_PASSWORD
              valueFrom: { secretKeyRef: { name: storeql-postgres, key: PRODUCT_DB_PASSWORD } }
            - { name: JAVA_TOOL_OPTIONS, value: "-Xms128m -Xmx512m -XX:+UseG1GC" }
            - { name: STOREQL_ADVERTISE_HOST, value: "product-svc" }
            # Backs the findProduct cache-aside read (ProductRepository).
            - name: STOREQL_REDIS_PASSWORD
              valueFrom: { secretKeyRef: { name: storeql-redis, key: REDIS_PASSWORD } }
          startupProbe:
            httpGet: { path: /health/started, port: 8080 }
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /health/ready, port: 8080 }
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet: { path: /health/live, port: 8080 }
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            requests: { cpu: 200m, memory: 450Mi }
            limits: { cpu: 500m, memory: 700Mi }

---
apiVersion: v1
kind: Service
metadata: { name: customer-svc, namespace: storeql }
spec:
  selector: { app: customer-svc }
  ports: [{ port: 8080, targetPort: 8080 }]
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: customer-svc, namespace: storeql }
spec:
  replicas: 1
  selector: { matchLabels: { app: customer-svc } }
  template:
    metadata: { labels: { app: customer-svc } }
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - { name: tmp, emptyDir: {} }
      containers:
        - name: customer-svc
          image: ghcr.io/red2n/storeql-customer-svc:latest
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
          ports: [{ containerPort: 8080 }]
          envFrom:
            - configMapRef: { name: storeql-common-env }
            - secretRef: { name: storeql-config-token }
          env:
            - { name: STOREQL_DB_USER, value: "customer_svc" }
            - name: STOREQL_DB_PASSWORD
              valueFrom: { secretKeyRef: { name: storeql-postgres, key: CUSTOMER_DB_PASSWORD } }
            - { name: JAVA_TOOL_OPTIONS, value: "-Xms128m -Xmx384m -XX:+UseG1GC" }
            - { name: STOREQL_ADVERTISE_HOST, value: "customer-svc" }
          startupProbe:
            httpGet: { path: /health/started, port: 8080 }
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /health/ready, port: 8080 }
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet: { path: /health/live, port: 8080 }
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            requests: { cpu: 150m, memory: 320Mi }
            limits: { cpu: 400m, memory: 512Mi }

---
apiVersion: v1
kind: Service
metadata: { name: notification-svc, namespace: storeql }
spec:
  selector: { app: notification-svc }
  ports: [{ port: 8080, targetPort: 8080 }]
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: notification-svc, namespace: storeql }
spec:
  replicas: 1
  selector: { matchLabels: { app: notification-svc } }
  template:
    metadata: { labels: { app: notification-svc } }
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - { name: tmp, emptyDir: {} }
      containers:
        - name: notification-svc
          image: ghcr.io/red2n/storeql-notification-svc:latest
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
          ports: [{ containerPort: 8080 }]
          envFrom:
            - configMapRef: { name: storeql-common-env }
            - secretRef: { name: storeql-config-token }
          env:
            - { name: STOREQL_DB_USER, value: "notification_svc" }
            - name: STOREQL_DB_PASSWORD
              valueFrom: { secretKeyRef: { name: storeql-postgres, key: NOTIFICATION_DB_PASSWORD } }
            - { name: JAVA_TOOL_OPTIONS, value: "-Xms128m -Xmx384m -XX:+UseG1GC" }
            - { name: STOREQL_ADVERTISE_HOST, value: "notification-svc" }
          startupProbe:
            httpGet: { path: /health/started, port: 8080 }
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /health/ready, port: 8080 }
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet: { path: /health/live, port: 8080 }
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            requests: { cpu: 150m, memory: 320Mi }
            limits: { cpu: 400m, memory: 512Mi }

---
apiVersion: v1
kind: Service
metadata: { name: reporting-svc, namespace: storeql }
spec:
  selector: { app: reporting-svc }
  ports: [{ port: 8080, targetPort: 8080 }]
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: reporting-svc, namespace: storeql }
spec:
  replicas: 1
  selector: { matchLabels: { app: reporting-svc } }
  template:
    metadata: { labels: { app: reporting-svc } }
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - { name: tmp, emptyDir: {} }
      containers:
        - name: reporting-svc
          image: ghcr.io/red2n/storeql-reporting-svc:latest
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
          ports: [{ containerPort: 8080 }]
          envFrom:
            - configMapRef: { name: storeql-common-env }
            - secretRef: { name: storeql-config-token }
          env:
            - { name: STOREQL_DB_USER, value: "reporting_svc" }
            - name: STOREQL_DB_PASSWORD
              valueFrom: { secretKeyRef: { name: storeql-postgres, key: REPORTING_DB_PASSWORD } }
            - { name: JAVA_TOOL_OPTIONS, value: "-Xms128m -Xmx384m -XX:+UseG1GC" }
            - { name: STOREQL_ADVERTISE_HOST, value: "reporting-svc" }
          startupProbe:
            httpGet: { path: /health/started, port: 8080 }
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet: { path: /health/ready, port: 8080 }
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet: { path: /health/live, port: 8080 }
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            requests: { cpu: 150m, memory: 320Mi }
            limits: { cpu: 400m, memory: 512Mi }
