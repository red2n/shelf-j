# Pulls non-secret config from the central config service (config-svc) at startup and layers it
# over local microprofile-config.properties defaults. No-op unless storeql.config.url is set.
com.storeql.service.ConfigServiceConfigSource
