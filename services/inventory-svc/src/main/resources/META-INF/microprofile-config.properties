# inventory-svc — defaults (local dev).
shelfj.service.name=inventory-svc
server.port=8004
server.host=0.0.0.0

shelfj.db.url=jdbc:postgresql://localhost:5432/shelfj
shelfj.db.user=inventory_svc
shelfj.db.password=inventory_dev_change_me
shelfj.db.schema=inventory

shelfj.consul.enabled=true
shelfj.consul.host=localhost
shelfj.consul.port=8500

shelfj.kafka.enabled=true
shelfj.kafka.bootstrap=localhost:9092
shelfj.outbox.poll-seconds=5

# Gap #50 — POS→SIM sync: order event topics consumed by inventory-svc
shelfj.kafka.topics.order-fulfilled=shelfj.order.order-fulfilled
shelfj.kafka.topics.order-returned=shelfj.order.order-returned

# Reservation lifetime (abandoned-cart auto-release) + sweeper interval
shelfj.inventory.reservation-ttl-seconds=900
shelfj.inventory.sweeper-seconds=30

# OpenTelemetry — OTLP/gRPC to the collector (override endpoint in docker-compose for containerised stack)
otel.sdk.disabled=false
otel.service.name=inventory-svc
otel.traces.exporter=zipkin
otel.exporter.zipkin.endpoint=http://localhost:9411/api/v2/spans
