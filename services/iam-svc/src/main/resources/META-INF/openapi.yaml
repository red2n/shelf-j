openapi: 3.1.0
info:
  title: IAM Service API
  version: "1.0"
  description: >
    Identity, authentication, and session management for Shelf-J. Issues and verifies JWTs,
    registers customers and staff, and manages POS cashier sessions (start/touch/end/idle sweep).
    Reached through the API gateway at /api/iam-svc.
tags:
  - name: Bootstrap
    description: One-shot platform-admin bootstrap for a fresh deployment.
  - name: Auth
    description: Registration, login, token refresh/logout, and password change.
  - name: Me
    description: The current authenticated principal.
  - name: POS Sessions
    description: Cashier POS session lifecycle and idle-timeout sweep.
components:
  schemas:
    Problem:
      type: object
      description: >-
        An error as RFC 9457 problem details (application/problem+json). type is a URN for the
        stable code (urn:shelfj:problem:CODE), title the code in words, status the HTTP status,
        detail the message for a person, instance the request path. code and details are the
        platform's extension members, requestId the correlation id, and error/meta the envelope
        members clients read before problem details.
      required: [type, title, status, code]
      properties:
        type: { type: string, format: uri, example: "urn:shelfj:problem:VALIDATION_FAILED" }
        title: { type: string, example: "Validation failed" }
        status: { type: integer, example: 400 }
        detail: { type: string }
        instance: { type: string, example: "/api/order-svc/orders" }
        code: { type: string, example: "VALIDATION_FAILED" }
        details: { type: array, items: { type: string } }
        requestId: { type: string }
        error:
          type: object
          properties:
            code: { type: string }
            message: { type: string }
            details: { type: array, items: { type: string } }
        meta:
          type: object
          properties:
            requestId: { type: string }
            nextCursor: { type: string }
  responses:
    Problem:
      description: An error, as RFC 9457 problem details.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
security:
  - bearerAuth: []
