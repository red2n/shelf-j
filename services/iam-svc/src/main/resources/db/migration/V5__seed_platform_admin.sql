-- Placeholder migration (slot reserved). Platform admin is bootstrapped via
-- POST /bootstrap/admin (BootstrapResource) which uses the live Argon2 hasher.
-- This file intentionally left as a no-op so the Flyway version sequence stays contiguous.
SELECT 1;
