# Test-only overrides — loaded by @HelidonTest via MicroProfile Config (test classpath takes priority).
# The breached-password screen calls the Pwned Passwords API; the ITs run without the network.
storeql.iam.password.breach-check.enabled=false
