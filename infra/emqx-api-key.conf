%% EMQX HTTP Management API bootstrap key — lets iam-svc kick a user's MQTT session on logout
%% (see com.shelfj.iam.client.MqttSessionRevoker) without going through the dashboard UI first.
%% Format/behavior NOT independently verified against a running broker (no Docker available in
%% the environment that authored this) — MqttSessionRevokerIT (Testcontainers, iam-svc) is the
%% actual verification; run it before relying on this outside local dev.
%% Dev-only credential (same convention as infra/pgbouncer-userlist.txt) — override via a real
%% secret store for any non-local environment.
iam-svc:iam_mqtt_admin_dev_change_me
