# Mosquitto — local-dev MQTT broker for notification-svc's device-facing push channel
# (shelfj.notification.channel=mqtt). Anonymous, unencrypted: fine on the docker-internal
# shelfj_network for dev; a real deployment would front this with auth + TLS.
listener 1883
allow_anonymous true
persistence false
log_dest stdout
