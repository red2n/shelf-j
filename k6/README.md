# k6 suites

End-to-end tests that drive the running stack through the gateway, the way a browser, till or
integration would. Every call carries a real JWT; the gateway strips client-supplied
`X-Tenant-Id` / `X-User-Id` / `X-Roles`, so a script that sends those instead of a token tests
nothing.

**The flow guard is the heart of StoreQL**, and it has two suites of its own (below). Run them after
any change to onboarding, tenant/store status, authorization, carts, orders or POS sessions.

## Running

Start the stack (`docker compose up -d --build`), then from the repo root:

```bash
k6/run.sh                             # flow guards, every functional suite, gateway checks
k6/run.sh flow-guard-runtime          # one suite (or several) by name
k6/run.sh --load                      # concurrent load: multi-tenant-retail, full-stack-simulation, rate limit
k6/db/validate_all.sh                 # after a run: what was written, and that every stored id is v7
```

`run.sh` reads the platform-admin login (`PLATFORM_ADMIN_EMAIL` / `PLATFORM_ADMIN_PASSWORD`) and the
gateway's rate limit from the environment, `.env` and the running container. It waits until every
service is routable through the gateway (a container can be healthy before Consul lists it),
prints one line per suite, keeps full logs in `$K6_LOG_DIR` (a temp dir by default) and exits
non-zero if any suite fails. `BASE_URL` points it at another gateway (default
`http://localhost:8090`).

| Suite | What it proves | Checks |
|---|---|---|
| `flow-guard-comprehensive` | Onboarding → first sale in order: each step works once its prerequisites exist and is refused when too early, by the wrong role, or against another tenant (spoofed `X-Tenant-Id`, stranger on `/onboarding/*`, idempotent receipt and sale, currency, reservations) | 110 |
| `flow-guard-runtime` | Transact-or-not: suspending a tenant or closing a store stops the gateway storefront and checkout, staff login and refresh, POS sessions, carts and orders — for that tenant or store only — and reopening restores them; a store id from another tenant is never operational | 79 |
| `iam-crud` | Sign-up, login, refresh rotation and theft detection, logout, password change, account deletion, staff provisioning, POS sessions | 52 |
| `tenant-crud` | Tenant profile, stores (cursor pages), zones, delivery areas and routing, staff, inventory settings, storefront config, platform console | 78 |
| `product-crud` | Brands, categories, products, variants, UOM, item templates, revisions, containers, attribute groups, category sets, bulk import | 91 |
| `inventory-crud` | Receive, adjust, ABC, safety stock, cycle counts, lots, serials, transfers and the rest of inventory | 125 |
| `pricing-crud` | VAT rates, price lists and their lifecycle, resolution, promotions and quotes, overrides, VAT return | 67 |
| `markdown-flow` | Date-code markdown (05.4, 03.9): the ladder read by a storekeeper as the default, refused to a storekeeper and a cashier, a duplicate step and 120 % refused, set by the owner and not seen by a rival; the plan refused to a cashier and to another store's keeper, a zero horizon refused, read by the storekeeper with the short-dated batch at the 2-day step (4.00 → 2.40) and the week-away one with no step, empty for a rival; the sticker refused to the till and to another store's keeper, an invented reason, a percentage and a price together, no reduction, a batch past its date and an unpriced product refused, three packs stickered by the storekeeper with a prefix-21 code carrying 2.40, shown on the plan, hidden from a rival; the cashier scanning it and a rival's till not, a made-up code unknown, the sticker on another product's line and four at a sticker for three refused with why, two at the sticker and one at the list sold on one ticket at 2.40 and 4.00 (8.80), the sticker counting down within 15 s, two more refused, the last sold, the sticker sold out at the till, the order keeping which line was stickered; a second sticker taking the next code, taking it off refused to the till, without a reason and to a rival, done by the storekeeper, refused again, the code dead, a sale naming it refused, listed under taken off and not at the other store | 56 |
| `promotion-scoping` | Promotion scoping (03.8): a promotion scoped to a category reaches every variant under it, parents included, because product-svc announces each product's category path and pricing-svc keeps it; a product moved out leaves the deal, a variant added later joins it; the catalogue can be re-announced for a projection that arrived late; mix-and-match bundles the dearest units and charges the rest in full — and the refusals and the abuse around each | 38 |
| `order-crud` | Order lifecycle (pay, fulfil, return, cancel, void), receipts, POS log, fiscal receipts, special orders, parked sales, gift cards, layaways, the online shopper, a catalog-mode order priced by a manager (SJ-D41), the online shopper | 123 |
| `notification-crud` | Shortage alerts raised by real stock movements, send with event dedupe, notification log | 21 |
| `notification-channels` | Notification channels beyond email (13.7): a text to a number and a push to the devices a shopper registered, both on the log with their channel; the shop reads which channels it has — with the wrong number, the wrong channel, marketing where consent cannot stand, a guest registering, a shopper sending, a rival shop, a full device list, the same token twenty times and thirty texts to a bad number | 44 |
| `reporting-crud` | On-hand, supply/demand, movement stats and sales reports fed by a real receipt and a paid sale; sales by category (19.x): a product under a two-deep category tree sold at the till is read by its own category with its share and rolled up to the parent, filtered by store and channel, a bad level refused, a rival's report without it, a storekeeper refused | 35 |
| `order-proposal-flow` | The automatic order proposal (06.x): ten beans on order from a supplier at 2.50, seventy received and sixty sold at the till, the month's demand aggregated and a reorder point computed; a run raises one DRAFT on that supplier with the EOQ at the price last paid and the arithmetic on the line, a typed line carries no reason, a second run waits for the draft, the runs read back, the draft cancelled and proposed again; a keeper of another store, covers of 0 and 400 days, a store that is not an id, runs read without a store, a rival's empty read and no token refused | 26 |
| `demand-forecast-flow` | The statistical demand forecast (06.x) through the gateway: a week of the beans on promotion and the store's promotion windows read from pricing-svc as staff (the read the forecast makes for its uplift), the run for a store that opened today says honestly it has no history yet and nothing seasonal or promoted, the horizon defaults to four weeks, an empty list rather than an error, no forecast for the beans yet and it says so; a reorder plan computed with the forecast seam in place; a horizon of no days or past a year, a store that is not an id or not named, a keeper of B refused A three ways and served their own, an owner of two stores asked to name one, a rival's read empty, no token | 31 |
| `gateway-smoke-it` | Health, a public contract, sign-up, login and an authenticated call through the gateway | 7 |
| `privacy-flow` | The login-to-customer link, loyalty and the confirmation email on a web order, marketing consent and the opt-out link, the data export and erasure — each step with the wrong caller (guest, another shopper, a rival tenant's owner) and the wrong input beside the right one | 75 |
| `storefront-account` | The shopper's own account on the storefront (12.10): the profile and the address book, keyed on the token's login and reachable by nobody else — with the guest, the other shopper, the rival shop, the wrong input, a full book and a race for its last place tried beside the right calls, and a delivery order placed with a saved address to close the loop | 44 |
| `compliance-flow` | The shop-floor legal records: age checks written by a cashier (refusal with reason, pass with what was shown), refused for the wrong shape, the wrong store, a shopper or a guest, and the register and its counts read by management only; the weighing-instrument register — registered, verified, repaired and out of trade, re-verified, out of service, retired — with every write refused to a cashier and every read to another store's cashier or a rival tenant; the legal receipt series — a prefix set by the owner and refused to a cashier or for a bad shape, a till sale numbered under it, the till reading the number in one held request (`?wait=`) and getting 404 before payment, the audit intact and management-only; the hash chain intact from the first document, the register exported as CSV with both hashes and as JSON with the lines, refused to a cashier; then the fiscal regimes (18.5) — a German business in EUR at 19%: a cashier and a rival refused at the settings, an unknown regime, a missing tax number (registering no module), a missing module, an unknown provider and a bad client id each refused by name, the store placed under KassenSichV with a simulated module, a cash sale read back with transaction 1 / signature 1 and `Beleg^11.90_0.00_0.00_0.00_0.00^11.90:Bar`, a card sale with 2 / 2 and `:Unbar`, the twelve-field QR, the audit and chain intact, the DSFinV-K zip produced and refused to a cashier, an unknown format refused, a stamped sale voided keeping its stamp; a Portuguese business at 23%: a bad NIF refused, and either refused by name without a signing key on the server or — with `STOREQL_FISCAL_PT_PRIVATE_KEY` set — placed under certified-software rules, two documents signed and chained with ATCUD 1 and 2, and the SAF-T (PT) file produced with both hashes and the certificate number; then product safety information (01.12) — a German business refused online without its manufacturer, EU responsible person and warnings and told what is missing, a French manufacturer needing no responsible person and a Chinese one needing one, a guest reading it with the offer, a till-only product refused going online until stated, the statement of an online product not thinned, a British business not bound, the missing list, a cashier, a rival, a shopper and a guest refused, a script, an http link, SQL, an overlong name and a contradiction refused by name, a bulk import's online row refused, and twenty at once putting a product online and thinning its statement never leaving it online without; then unit pricing (03.13) — a 750 ml bottle's price per litre on a guest's quote once pricing-svc has the measure, the same on the basket line and a cashier's shelf label, shoppers and guests refused labels, no, too many and SQL label ids refused, a priced item without a measure listed and cleared once declared, food refused without a measure where the law applies and accepted at 1 EA, a German business told a unit price is law, 375 g of a price per kilogram quoted rather than refused (SJ-D55), and twenty concurrent measure saves settling on the stored measure; then the prior price (03.12) — a German price reduced the day it was set answered `SHORT_HISTORY` with its prior price reported, its shelf label, the reductions list and the storefront banner all refusing to announce it, the applied-price history newest first and certain, refused to a cashier, SQL and a rival, a bad channel refused, a short-dated sticker at the German till showing its original price and a damaged pack none, a British reduction announceable and advertised, and twenty concurrent price sets settling certain on the price a shopper is offered | 240 |
| `cash-limit-flow` | Cash refused at the limit the law sets where the store trades (09.17): the register lists France's EUR 1,000 in force and the Union's EUR 10,000 upcoming (in force when asked about 10 Jul 2027), Germany's upcoming cap alone, India's two lakh rupees in rupees, Britain's none; a French till refuses EUR 1,200 in cash naming the law, takes 999.99, refuses a penny more for the same goods, takes the balance by card, refuses exactly 1,000 for owner and cashier alike; a German till takes cash today, an Indian till refuses INR 250,000 and takes 199,999, a British till takes anything; abuse: cash in a currency the limit does not name, a rival's sheet, a rival's owner paying our sale | 23 |
| `deposit-return-flow` | Deposit return schemes (09.16): the register lists Germany's Pfand in force and the UK scheme upcoming from 1 October 2027; the catalogue records a drink's container; a German till puts EUR 0.25 on each bottle as its own line beside the drink, added to the total, taxed as the drink; a British till charges none today; empties come back for the scheme's amount, once per till key, and the drawer carries the pay-out; the period's report shows charged, refunded and unredeemed by material. Refused: a container the scheme does not take back, no lines, too many of one kind, a refund where no scheme is in force, no till key, a material without a volume; abuse: a rival's owner at our till, a rival reading our refund, a cashier reading the report, a period ending before it starts. | 44 checks |
| `item-lifecycle-flow` | Item lifecycle (master data): a NEW_LINE is listed with its launch day, hidden from the shop and refused at the till naming the day; launched it lists and scans; DISCONTINUED it still sells while stock lasts but leaves the low-stock report and the planning run raises nothing for it; reinstated it is back; delisted it is gone from the shop and the till. Refused: a move a line cannot make from where it is, a launch day on a line on sale, an unknown status, a cashier's move; abuse: a rival's owner moving our line. | 37 checks |
| `script-integrity-flow` | Script integrity on the pages that lead to payment (PCI DSS SAQ-A, 6.4.3, 11.6.1): the web shell serves an inventory of every script it loads with a reason and a SHA-256; every script the entry page loads is in it, none from another origin, none inline; the bytes served match; the gateway's monitor, run by a platform administrator, reads the shell and finds no drift; a business owner is refused, an anonymous caller is refused. Needs `WEB_URL` (default http://localhost:8088). | 18 checks |
| `password-policy-flow` | The password policy to NIST SP 800-63B-4 / ASVS 5.0 V6: fifteen characters where the password is the only factor, any character, no composition rule, at most 128; the login is not a password; a phrase known from breaches is refused through the Pwned Passwords range lookup (five characters of the hash leave the service; a screen that cannot be reached lets the password through); the same at sign-up, on change and when staff are provisioned. Refused: fourteen characters, the login, a breached phrase, 129 characters; abuse: a change to a breached password, a cashier provisioning staff. | 17 checks |
| `problem-details-flow` | Machine-readable errors and a current API description: every error — from an exception mapper (400 invalid UUID, 404, 409), from bean validation, from the gateway's JWT filter (401) and the authorization filter (403), and for an unknown service — leaves as RFC 9457 `application/problem+json` with a URN `type` for the code, the code in words as `title`, `status`, `detail`, `instance`, the `code`, and the envelope members a client read before; a response with data is untouched; every service and the gateway publish OpenAPI 3.1 with the Problem schema. | 45 checks |
| `token-signing-flow` | Asymmetric token signing with key rotation (20.15; RFC 7517, RFC 8725): an access token is RS256 and names its key; the public halves are a key set anybody may read (cacheable, no private parameter) and the gateway verifies against it, holding no secret. Refused: HS256 tokens keyed with the public key in three forms (algorithm confusion), `alg: none` with and without a borrowed signature, a payload raised after signing, an unknown key id, no key id, forty unknown key ids in a row; the key set cannot be written; the key register and rotation are the platform administrator's (owner, cashier, anonymous refused) and show no key material. Rotation: the new key is published at once and signs after its lead time, a login inside the lead is signed by a key already known, earlier tokens go on working, and the gateway accepts the first token the new key signs without a restart. | 41 checks |
| `mfa-flow` | Multi-factor authentication (20.12; NIST SP 800-63B-4 AAL2, ASVS 5.0 V6, PCI DSS 8.4): an authenticator app set up (a secret and an `otpauth://` URI; no factor until a code confirms it) and then owed at every sign-in — no token until it is answered — with `amr` in the token and kept across refresh; recovery codes that work once; a passkey registered and used with a software authenticator (WebCrypto P-256: attestation `none`, user verification required), the token saying `hwk`; a business requiring a factor of a tier — a password-only session is not renewed (`MFA_REQUIRED`), the next sign-in gets a token with scope `mfa-enrol` that reaches only the second-factor routes (`403 MFA_ENROLMENT_REQUIRED` elsewhere) and the set-up answers the real session; the lost-phone reset (owner for their staff, platform administrator for anybody); the platform administrator born with a factor that cannot be removed. Refused: a code twice, another login's code, the answer after five wrong ones, a client-sent scope header, the last factor removed against the rule, a registration or an assertion for a look-alike origin, a stranger's authenticator claiming the credential, an assertion replayed, a cashier or manager changing the rule, an owner resetting themselves or a stranger. The suite keeps a successful sign-in between its deliberate failures and `run.sh` clears the gateway's counters after it. | 70 checks |
| `sso-flow` | Single sign-on through the business's own identity provider (20.x; OpenID Connect Core 1.0, RFC 7636, RFC 9700), against a real OpenID Connect server (mock-idp): an owner connects the provider and the platform reads it, a cashier signs in there and comes back with a ticket the app trades for a session, a business requires its provider of cashiers and their password stops working, the provider's own second factor is enough — refused: a ticket twice, a ticket without the verifier that started it, ten redemptions of one ticket at once, a state twice, a return to anywhere but the app, a person the business never added, another business's staff | 65 |
| `api-keys-flow` | A business's own API keys (22.7): the owner mints a key for its warehouse system as a storekeeper of one store, shown once with its prefix and listed without itself, a manager reads the list but cannot mint or revoke, never an owner's or the platform's key, an expiry in the past and a store that is not an id refused, another business sees none; the key receives stock at its store and not another, reads what it keeps, cannot do a manager's work, and its use is remembered; it cannot sign in, mint keys, set up a second factor, start a business, act for the platform or read what only an owner may; a key a character off or the wrong length opens nothing, nor another business's stock; a manager or another owner cannot revoke, the owner does, once, and the key stops within seconds | 31 |
| `webhooks-flow` | Webhooks to a business's own systems (22.6), against the stand-in receiver `webhook-sink`: a manager reads the event catalogue, the owner registers an endpoint for stock and orders and is shown its secret once, listed without it; a manager registering, an unknown event, an empty subscription, a private address, plain HTTP and credentials in the URL refused, another business seeing and reading none of it; a ping lands within seconds, the receiver's echo shows the Ping signed under the secret over the body as sent with an envelope naming the delivery, another business cannot read the delivery; stock booked in through the API arrives as a StockReceived delivery, signed, the event whole, and the endpoint remembers its last success; a receiver nobody answers is tried, queued again a minute out with the reason, sent again by hand, not by another business, and removed with its log by the owner alone; a rotated secret signs the next delivery and the old no longer does; switched off nothing is sent, switched on what waited goes, and the log reads newest first | 35 |
| `api-versioning-flow` | The API versioning policy (22.8; RFC 9745, RFC 8594): `GET /api/versions` needs no credential and says v1 is current at `/api/v1/{service}`, the unversioned alias is deprecated since 23 Sep 2026 with a sunset of 30 Sep 2027 and `/api/v1` as its successor, the policy in a paragraph and where a description is; every answer on the alias, a refusal included, carries `Deprecation: @1790121600`, `Sunset: Thu, 30 Sep 2027 00:00:00 GMT` and a `Link` to the successor, and the versioned form carries none; a version nobody published is `404 API_VERSION_UNKNOWN` as a problem with a `Link` to the latest; an authenticated read and write answer the same on either form; every service describes itself in OpenAPI 3.1 under `/api/v1/{service}/openapi` and the gateway's own description lists `/api/versions` | 32 |
| `sandbox-flow` | A business's sandbox (22.8): none to start, a manager cannot make one, the owner makes a tenant of its own marked SANDBOX naming the live business on the SANDBOX plan, one at a time, read by a manager, the live business still live; the platform sees it for what it is, the SANDBOX plan exists sold to nobody and off the price list, and the live business cannot be put on it; the owner trades its token for one in the sandbox (an owner there, `amr` sandbox, no refresh), a manager is not let in, no sandbox of a sandbox, inside the profile and plan are the sandbox's with the live default store copied in; a `sqk_test_` key minted from the live business and one minted inside are both sandbox keys, listed beside the live keys from either side; the key creates a product the sandbox sees and the live business does not, and is no more the platform than any key; a webhook registered inside is delivered and an in-app message is logged there and nowhere else; a manager cannot remove it nor is it removed from inside, the owner does, switched off with the reason, the key stops within seconds, from inside the sandbox reads as switched off for what is left of its token, and there is nothing to enter; another is made at once, a fresh one, entered now, without the old one's product | 46 |
| `accounting-connector-flow` | Accounting connectors (17.9), against the platform's SIMULATED package: a manager reads the four packages that can be connected with the settings each needs, a cashier cannot; nothing connected to start with; a manager cannot connect, an unknown package, Xero without its organisation or its token and a day that is not one are refused; the owner connects the stand-in pushing from a week ago, with nothing kept of a token, read by a manager and by no other business; the package's chart is read and stock mapped onto it, a mapping from a non-code refused; a manual journal lands with the package's own id and one try while one the package refuses waits with the reason and a next try a minute away, read whole with its lines and tries, filtered by status, unseen by another business or a cashier, and a second pass pushes nothing twice; a goods receipt's posting goes like any journal; mapped onto a known account and queued again by a manager it lands on the next pass; unmapped again the next such journal is refused and the owner leaves it out with a reason, a reason being required and a delivered journal never pushed again; switched off nothing is pushed, switched on what was posted meanwhile goes; a journal is pushed by the clock with nobody asking; a manager cannot disconnect, the owner does and the log goes with it | 45 |
| `role-model` | The role model (20.10): a tenant defines roles of its own on the built-in tiers — a shift lead who cannot void a sale or post a journal, a trainee who cannot open the drawer — assigns staff to them and their tokens carry the permissions; every gated decision refuses the narrowed role by name and admits the plain one; a role redefined reaches its holders at their next sign-in; a role in use cannot be deleted; a removed assignment leaves the login (SJ-D51); a forged permission header is stripped at the door; twenty definitions of one code at once produce one role | 59 |
| `audit-trail` | The business audit trail (20.11): every discount, void, no-sale, cancel and return on the shop floor read back as one stream naming who did it, with the stock adjustment the unified screen reads beside it — and the refusals: the wrong role, the wrong tenant, the wrong input, a hammering cashier, a lifted cursor | 34 |
| `uuid-v7-flow` | Ids are RFC 9562 UUIDv7, in and out: every id the platform hands back is a canonical v7; every door an id comes in by — a path, a query parameter, a body field, a string parsed by hand, the event id of a message — refuses any other version, the wrong variant and any other spelling with 400 INVALID_UUID before anything is looked up; a v7 that names nothing is still a 404. The Idempotency-Key, header or legacy body field: a clock reading or a v4 is 400 IDEMPOTENCY_KEY_INVALID; a v7 places the order once however often it is sent — in upper case, and ten times at once — the SJ-D70 case made impossible | 69 |
| `chargeback-flow` | Chargeback and dispute handling (11.9): a card payment taken at the till is charged back — a manager records the acquirer's notice (the local stack runs the MANUAL provider; a provider's own disputes arrive by signed webhook and are covered by `DisputeIT` and `StripeDisputeParsingTest`) — the business is told at once and by when it must answer; the ledger moves the amount out of card clearing (1250) into card receipts in dispute (1255) and books the acquirer's fee (6511); the answer is given once and in time; a dispute won brings the money back, one lost or accepted writes it off (6510); the register pages, filters, and reports the ratio the card schemes watch. Refused: cash charged back, more than was paid, a second dispute while one is open, an empty answer, an answer twice or after its date, an outcome decided twice, another business's payment or dispute, a cashier, an anonymous caller. | 47 checks |
| `settlement-flow` | Reconciliation against the acquirer's settlement file (11.10): card payments taken at the till — one part-refunded, one charged back — are paid out and the payout's file imported; every line matching, it is reconciled at once and the ledger empties card clearing (1250) into the bank (1200), books the acquirer's fees (6500) and does not book the chargeback's fee twice; a second payout with a payment settled short, a reference typed wrong, money that answers to nothing, a payment settled twice and a reserve held back waits for each line to be decided and for a sign-off, and only then reaches the ledger, what nobody could place going to unallocated receipts (1299); the payments no payout has covered are listed until one does; Stripe's and Adyen's layouts name their payout, date and currency themselves. Refused: a file that does not add up, the wrong layout, a refund written as money in, a payout imported twice or in another currency, a full card number in a file (at the gateway), a file past the line cap or the route's size cap, another business's payout or payment, a line decided after the books have it, a cashier, an anonymous caller; ten imports of one payout at once make one batch. | 75 checks |
| `plans-flow` | Plans and packaging (21.8): the platform writes a price list — a draft, a price, what it includes, on sale, then the plan a business signing up starts on — and a business that signs up lands on it and sees what it is allowed against what it is using. The limits then bite where the thing lives: stores and staff refused by tenant-svc from its own data, products by product-svc reading the allowance from tenant-svc; the staff limit counts people, not the stores they work at. A bigger plan lifts the ceiling; moving back down while the business is over it is refused, naming what is over. A retired plan keeps its subscribers and is sold to nobody, and a business signing up with nothing on sale is unrestricted and says so. Refused: a plan with no price, a draft as the default, a duplicate code, a promise nobody enforces, a limit written as a yes or no, a bad currency or date, an owner writing the price list or putting itself on a plan, a cashier reading it, an anonymous caller. | — |
| `tenant-limits-flow` | Per-tenant rate limits and storage caps (21.11): the platform sells a request rate and two storage caps, each naming the door that refuses; a business lands on the plan and sees them; five 200 KB images fit in a megabyte and the sixth is refused before a byte is stored, naming the figure and what it would make, a replacement measured as the room it frees plus the room it takes, a deletion making room; a plan keeping no documents refuses a supplier e-invoice unread; within the plan's minute the business is told to wait with the code and a Retry-After, its shop's guests wait with it, the neighbour's minute is its own, and the platform's administrator is counted by nobody | 17 |
| `signup-trial-flow` | Self-serve signup and trials (21.13): a prospect reads the price list with no login — the price, what it includes, the trial and which plan is the default — with a draft and a plan sold by hand not on it and the platform's own list still private; a business chooses a plan as it signs up and starts on that plan's trial with nothing owed, a plan not on sale or sold by hand refused before any business exists, and a second signup on the same login refused as a second trial; three days before the trial ends the owner's address gets the day and the price, once; the day it ends the first invoice is raised and announced with a link that pays it with no sign-in, once; the subscription is then active and billed | 22 |
| `health-probes` | The probe separation a pod's life depends on: liveness carries no database check on a business service or on the gateway, readiness carries one plus the event consumers, and the aggregate `/health` carries the lot — which is why no liveness probe may be pointed at it, since a slow database would then kill every pod and every replacement in turn. Drives the deep check `/admin/health`: a real round trip with its timing, the consumers, the connection pool's own figures — the one way to tell an exhausted pool from a dead database — and what readiness is currently saying. Refused: a cashier reading the inside of a service, an anonymous caller, and the probes themselves, which are not published to the internet. | — |
| `billing-flow` | Subscription billing and invoicing (21.9): the platform fills in who it invoices as — nothing is billed until it has — sets the rates it has to charge, and a business that signs up is subscribed to the plan it landed on and billed for it **in advance**, at the price it was sold. The invoice is numbered gaplessly, adds up, carries the treatment its country and VAT number decide, and prints both sides as they stood that day. A mid-period upgrade bills **two lines**, a credit for the unused days at what was billed and a charge for the same days at the new price; a downgrade waits for the period already paid for. Money settles an invoice; an unpaid one is withdrawn with a reason and keeps its number. A business the platform has no rate for is **passed over and named** by the run rather than stopping everybody else's invoices, while that business's own upgrade refuses loudly. Refused: a business recording its own payment or withdrawing its own invoice or reading everybody's receivables, another business's invoice (404, not 403), a cashier, an anonymous caller, and a run for a day where the test clock is off. | — |
| `dunning-flow` | Dunning and suspend-for-non-payment (21.12): an invoice goes past its date and the platform chases it on the schedule the policy names — a notice at each offset, then the service interrupted, then the debt given up on — and a run that has not run for days owes the business **every** notice it missed, once, because suspending a business the platform never finished telling was late is the opposite of what dunning is for. Safe to run twice: a step happens once by a unique index. The way back works **without a login**, because a suspended business cannot sign in: the pay link in every notice pays one named invoice, and a link for an invoice already settled opens nothing. The check that matters most is the last one — **paying up brings back a business the platform suspended, and does NOT bring back one an administrator switched off**. Refused: a policy whose stages are out of order, a due date moved inwards, a business reading or running the platform's own chasing, an anonymous caller. And the notice itself (SJ-D68): each reminder is an email on the owner's log at the sign-up address (SJ-D72), naming the invoice and the sum, in the platform's name, saying when the service goes and carrying the pay link; the second notice's link replaces the first's; the suspension is a notice too, read once the business is back. | 49 |
| `statutory-returns` | Statutory reporting by jurisdiction (07.14): what a business owes each authority, when each return falls due, and the evidence that it went. Every date and state is **derived on read**, so the suite asks the same calendar as of different days and checks the state moves on its own — nothing owed inside the period, owed the day it ends, still `DUE` on the day it falls due (filing on the day is filing on time), `OVERDUE` after it, and `FILED` whatever the date says, because a return filed late is filed. Which returns reach a business is asked of the **period**, not of today: a Portuguese business owes the monthly SAF-T by the 5th and the EU statement by the 20th, a British one the quarterly VAT return on the 7th HMRC publishes and **no** EU statement, because it stopped being a member in 2020. A return the platform cannot produce says so rather than offering an empty export, and one it can links to the service that owns the bytes. A correction names the filing it replaces and both stay on the record, with exactly one standing. Refused: a period that has not ended, a date that does not begin one of that return's periods, an unrecognised route, an unknown return, filing twice without naming what is corrected, correcting a filing that is not the one standing, a cashier, an anonymous caller, and an `asOf` that is not a date. | — |
| `gs1-scan` | GS1 2D barcodes at the till (07.15): the one route that reads every kind of code. The assertion that matters is that **one item is found by every form of its GTIN** — the EAN-13 a shop typed off the shelf edge, the GTIN-14 a case carries, a DataMatrix element string, the bracketed form a person writes, and a Digital Link QR on GS1's resolver or on the brand's own domain. What such a code carries is then used rather than discarded: the lot, the expiry (including a day of `00`, which means the end of that month), the net weight scaled by the AI's own decimal places, and an amount payable — with the currency-qualified form (393n) giving the amount and not the three ISO 4217 digits in front of it. A shop's own internal code goes on working and reports that it carried nothing. Refused: one digit out in the check digit, a GTIN nobody carries (naming the GTIN, not the element string), an application identifier the platform does not read — refused rather than skipped, because skipping means guessing where its data ended — a blank code, an ordinary web page, and another business's item by any encoding. | — |
| `terminal-payments` | EMV terminals and pinpads (07.16): the till stops taking a card on trust. The check that matters is the **double charge** — a second press with the same `Idempotency-Key` is answered with the *first* attempt and its own authorisation code, with one attempt on the record however many times the button was pressed, because that is how a real customer is charged twice. The other outcomes are asserted apart: a decline settled with the machine's own words and nothing printed, a cancellation that is not an approval, a fault that is settled rather than left waiting, and a **timeout** that is neither — no tender is recorded, because the platform does not claim money it cannot prove it took, but the attempt keeps its reference, which is how the charge is traced in the acquirer's settlement file. An approval carries what a card receipt must have (scheme, four digits, authorisation code, AID, entry mode) and no more of the number exists anywhere. Money goes back only on the card that paid, never more than it took, and never against an attempt that took nothing. Refused: a label shaped like a card number, a make the platform cannot talk to, two machines sharing a label in one store, a retired machine, an amount of zero or negative or with a third decimal place (refused, not rounded), a cashier putting money back on a card or reading the register, another business taking a card on this one's machine by naming its id, and an anonymous caller. | — |
| `merchandising-range` | Shelf space and range (07.17, 07.18): the two decisions a buyer makes about a shelf — how much of it a line gets, and which shops carry the line at all. The assertion that matters for space is that a layout is **checked against the furniture it is drawn for**: facings times a variant's recorded width either fits the shelf or is refused, and refused when the layout is saved rather than at publication, because a layout found not to fit after a reset has been scheduled around it is found out too late. A line with no recorded width is still placed and the answer says how much it could not account for. Publishing supersedes rather than edits, and the capacity it implies **crosses Kafka into inventory-svc**, where the shelf-gap report answers what it would take to fill the bay — the number a reorder level cannot give, because 20 units is plenty for a bay holding 12 and a gap in one holding 60. For range, a change is **intent until it is applied**: recorded with a date and a reason, doing nothing until the sweep runs on the day, and applied exactly once. A cluster's membership is read on the day the change is applied, so a shop that opened since the decision is included. A closing review produces the changes its decisions imply, carrying the figures it was decided on, and de-lists a product only when every one of its variants was reviewed and every one was dropped. Refused: a layout wider than its shelf, a shelf the fixture does not have, a second draft for one fixture, publishing an empty layout, editing a published one, a de-list against a line that is ranged everywhere (which cannot be expressed at all, and stays due rather than being swallowed), a change aimed at an empty cluster, a change with neither target or both, a change with no reason, an unknown action, two clusters sharing a code, closing a review with a line unread, dropping an own-brand line with no note, money with no currency, a cashier re-laying a shelf, another business reading this one's layouts, and an anonymous caller. | — |
| `ereporting-flow` | E-reporting, the second limb of France's reform (18.9): the transactions no invoice covers. What only a live stack proves is the **seam between two services** — tenant-svc derives the calendar (both returns present for a French business and neither for a British one, at the new ten-day frequency, each due ten days after its period) and holds what was filed, while order-svc builds the data, transmits it and answers with the payload's SHA-256, which is then recorded as the filing so the bytes and the calendar are tied together without tenant-svc holding a copy. The period reported here is one with **nothing in it**, which is a case worth having: an empty period is transmitted as a *néant* in words rather than skipped, because silence is indistinguishable from a platform that stopped working. (The aggregation itself — scope decided by the absence of an e-invoice, the day taken from the fulfilment history, refunds as negative operations — is asserted in order-svc's integration tests, where a sale can be backdated into a period that has ended; nothing in k6 can backdate a sale.) A correction supersedes its predecessor and both stay. Refused: a period that has not ended, a window that would sweep months into one filing, an unknown return, a business the duty does not bind (asked of the obligations sheet), a network that carries invoices and not reports, a business with no network at all, a period already reported, a cashier, another business reading this one's report, and an anonymous caller. | — |
| `workforce-flow` | The roster and the time clock (store operations & workforce): a shop's biggest controllable cost, which the platform kept no record of. The assertions that matter are the ones that cost somebody money — **one open entry per person**, so two taps on a slow terminal are one entry rather than two afternoons' pay; an **unpaid break coming off the hours** and a paid one not, which is the employer's arrangement kept rather than decided; an **open entry reported as open, not as zero**, because a zero looks like a day nobody worked; and a **correction that supersedes** the entry it replaces, with a reason, both staying on the record. Clocking is the person's own act at `/workforce/clock`, outside the `/admin/` prefix that is gated to management — a clock only a manager could press would be a clock nobody used — while a manager writing somebody's hours is a separate route recorded as `MANAGER`, so an audit can tell who pressed what. A rota answers what is worth saying about each person's own shifts (eleven hours' daily rest, a break expected after six, overlapping shifts) and is **never refused** for it, because member states implement the directive with their own derogations. Attendance counts turning up in **entries, not minutes**: somebody who clocked in and straight back out worked nothing but did turn up. Refused: a shift or a clock-in at a store the person is not assigned to, a shift rostered for somebody else, a second break, clocking out twice, a correction with no reason, correcting an entry already corrected, a cashier reading the management roster, another business touching these hours, and an anonymous caller. It then follows the money: a pay rate is recorded (dated, so a rise does not re-cost the past), hours are worked and a sale is taken, and the **labour report** is polled until the day shows both — the hop that needs three services running, since what an hour costs is tenant-svc's, the sales are order-svc's and the report is reporting-svc's. The event that joins them carries a store, a day and money and **no person**, so pay never leaves the service that keeps it. Labour as a share of takings is the figure a shop is run on; hours nobody had a rate for are counted and said to be uncosted rather than free. Also refused: two rates starting the same morning, a negative rate, a cashier reading what people are paid or the labour report, and another business seeing any of it. | — |
| `commission-flow` | Commission and sales attribution (store operations & workforce): an arrangement is recorded with marginal rate bands and somebody put on it from a day; the real tenant-svc rates a period's figures — 1,500 of sales under 1% to a thousand then 5% above it earns 10 + 25, stated band by band, and exactly at the threshold earns the lower band only, so crossing it re-rates nothing; somebody on no arrangement has their sales reported and earns nothing rather than being left out; a correction is a new version that leaves the old rates alone and moves the people across from a day, so a month spanning it is two segments each under its own rates, the new one starting its own band progression; a till sale is credited to the assistant the till named, or to whoever is operating it, and an online sale to nobody; a sale is re-credited with the reason kept; a statement for a finished period is drafted, approved (which freezes it, with who and when), restated by one naming it — the first standing until the restatement is approved — and a draft thrown away. Refused: no bands, a first band above zero, a per-unit scheme without a currency, an unknown basis, two arrangements on one day, a path that is not an id, a version already replaced corrected or assigned, withdrawing a scheme people are on, a seller named on an online sale, a re-crediting with no reason, a period still trading, a second statement for an approved period, approving twice, discarding what was approved, a status that does not exist; abuse: a cashier reading or writing any of it or crediting a sale to themselves, and another business reading, crediting or rating against this one's arrangements | 60 |
| `store-tasks-flow` | Store task lists and opening/closing checklists (store operations & workforce): an opening checklist with three lines, a single daily task, a weekly list naming its day and an ad-hoc one; a manager generates the store's day (two fall due today) and a second generation creates nothing, because the unique constraint decides; the cashier sees today's list at their store with what is still to tick, is refused finishing with required lines unticked, ticks two lines, is refused ticking one twice, finishes (done, late, the optional line left alone) and skips the bins with a reason; the manager's day reads one done late, one skipped, settled; a job is raised by hand once, the list withdrawn once, what was raised stands; the Mumbai store gets the business-wide lists for its own today with 00:01 falling due at 18:31Z the evening before. Refused: an unknown kind, a time that is not one, a day outside the week, a weekly list with no day, a blank line, a range too long, finishing what is finished, skipping without a reason, raising twice, raising a withdrawn list, a cashier writing the list or reading the manager's day; abuse: another business finishing, seeing or listing this one's work, and nobody at all | 42 |
| `broadcast-flow` | Store communications and broadcast (store operations & workforce): a notice to every store, pinned until withdrawn, seen unacknowledged by cashiers at both stores, acknowledged once (a second tap is a conflict), and the manager's reach naming who has not read it per store; a notice to one store and one role reaching the storekeeper there and neither the cashier beside them nor the other store; an urgent notice published, withdrawn with a reason, gone from the shop floor and kept for management. Refused: withdrawing without a reason or twice, acknowledging what is withdrawn or not addressed to you, an unknown priority, an expiry that is not an instant or is before publication, a store nobody holds, a blank title, a cashier publishing or reading the reach, somebody not on the store's staff acknowledging, a store-scoped login acknowledging at another store; abuse: another business seeing or acknowledging the notice, and nobody at all | 33 |
| `landed-cost-flow` | Landed cost apportionment (07.x): an order of apples and pears received, its batches carrying the order's price; freight of 10.00 spread by value as 6.25 and 3.75 with what each unit rose by, posted Dr Stock / Cr 2110, and inventory lifting the batches' cost through Kafka; duty by quantity landing on top; listed by receipt and by order; a retried charge the same charge; a reversal with a reason posting the mirror and taking the batches back to the penny. Refused: an unknown type or basis, a charge of nothing, another currency, a carrier or receipt nobody has, a reversal without a reason or twice, a cashier landing or reversing; abuse: another business seeing, landing or reversing a charge, and nobody at all | 30 |
| `message-templates-flow` | The business's messages in its own words (13.x): the catalogue of ten messages by audience, an order confirmation by email and push with its values, a recall notice keeping the law's six parts; a Polish draft previewed with sample values in Polish figures (`24,60 GBP`), an unknown value named by the preview rather than refused, and nothing saved by a preview; saved as version 1 then 2, the first kept retired; five saves racing for the push's next version, each saved or told another was, one live and numbered without gaps; the business's name as the sign-off until the owner sets one; a shopper who reads Polish ordering and written to in the business's Polish words, `pl`/`v2` on the log, signed as the owner chose; a shopper who never said getting the platform's English in pounds; the words retired and the Polish reader back on the platform's English. Refused: a value the message does not have (named in the refusal), a section never closed, a confirmation that never names the order, an email with no subject, a recall notice that leaves out what to do, the remedy and whom to call (`422`), a push longer than a phone shows, a language that is not an ISO 639 code (for a template, a house language or a shopper), a message or form the platform does not send, a body past forty thousand characters, retiring twice; a cashier, a storekeeper, a shopper or nobody; abuse: the rival shop reading or retiring this shop's words, and twenty oversized previews at once | 61 |
| `usage-metering-flow` | Usage metering and quotas (21.10): the platform prices a plan by what a business does — three orders and two text parts a month included, then 0.50 and 0.035 each, texts a hard ceiling; a business on it reads the period against the plan; four orders counted from order-svc's events, the first retried with its key still one, one beyond the allowance at 0.50 so far, 80% and 100% raised once each; an order never refused however far over; a 200-character text counted as the two parts the carrier bills; a marketing text to a shopper who agreed to texts refused past the ceiling while a text a customer must get still goes and is counted; the billing run at the period's end bills the next period in advance and the twelve orders and one text part beyond the plan in arrears, once however often it runs, and the period is written down against that invoice; the platform reads one business's usage and who is outgrowing a plan. Refused: an allowance for a meter nobody counts or for nothing, a hard ceiling on orders, a meter nobody counts on a plan, a negative allowance, a business pricing the platform or reading its alert list, a cashier, a shopper or nobody reading the usage; abuse: ten orders at once and one order retried ten times at once, counted as eleven exactly with no third alert, and another business's count and ceiling untouched | 44 |
| `dpdp-flow` | India's DPDP Act as a person meets it (13.12): the notice read before signing up, its languages and purposes, the Act's date; the owner names the grievance contact and the period (91 days refused, a bad email refused, a cashier refused); the notice published in English and Hindi, versioned, a Hindi reader served Hindi and a Tamil reader English and told; consent by purpose against the notice read, withdrawn in one call, the evidence read by the owner and refused to a cashier and a rival; a child refused marketing without a guardian, allowed loyalty, the guardian recorded (a document number refused, an unknown verification refused, a cashier refused) and withdrawn with marketing falling; requests opened and due within the period, a nomination naming somebody, the queue, the owner's answer, answered twice refused, a rival refused, twenty open at most; a platform breach notice carrying the business's three DPDP duties with the Board's report due 72 hours from the notice, the Board told recorded once, a GDPR duty refused, the customers told through the platform and recorded as the reference; abuse: a rival's notices, a shopper on the staff route, an unsigned shopper | 83 |
| `security-incidents` | Security incident, breach and exploited-vulnerability reporting (21.15): security.txt through the gateway and the web app, public, plain text, a real contact and an honest expiry, nothing written to it and its public path opening nothing else; an exploited vulnerability's 24 h and 72 h clocks from awareness, its final report waiting on a measure and then due 14 days after, recorded once, refused too early, closing refused while businesses are untold, nothing after the close; a severe incident flagged overdue on the register with its next stage, its final report a calendar month after the notification, no measure stage; a breach notice reaching the business named and not its rival, refused to a cashier and a guest, another business's acknowledgement a 404, acknowledged once; owners, cashiers, guests and a forged role header refused the register; nineteen bad inputs and SQL refused by name, no edit or delete; twenty recording one report at once record it once, twenty acknowledgements agree | 76 |
| `recall-buyers` | Recall notice to buyers (05.10): a shopper's online order, a till sale to a walk-in who left a number and an anonymous till sale of one lot; a recall with no remedy, no contact, a bad web address, a remedy the law does not name, a bad number or date refused by name, a cashier, a shopper and nobody refused to open one; in Germany one remedy without a reason and the word "precautionary" refused, one remedy with its reason kept, a British business not bound; the recall finding the three orders that drew on the lot, order-svc issuing a notice per order, two buyers identified and the walk-in kept unidentified, the walk-in's number never shown, the shopper's own notice with the product, the lot and the remedies and nobody else's, a bystander seeing none and no token refused, the written notice logged once with the headline, the lot, what to do, the choice and the contact and no soft words; a remedy not offered, one the law does not name, a bystander, nobody and the shopper at another shop refused, the refund chosen once and not changed, a shopper refused the list and the settlement, a refund refused by hand and through another order's return with nothing recorded, the return that refunds settling the notice and not twice, the returned jars held under the recall, twenty staff choices at once recording one, a replacement and a declined settled once each with their notes, another business refused, every buyer settled | 64 |
| `retention-flow` | Data retention (21.16): a British business's floor for transactions (six years, VATA 1994) and Germany's longer one arriving with a German shop; six years, one day under eight, a negative period, over a hundred years, words, no period, an unknown class and SQL in the class refused, the floor and longer allowed, a cashier, a storekeeper and a shopper refused; staff read the schedule, a shopper and nobody do not, a rival sees its own; twenty decisions at once all kept with one in force; four till sales (a customer's, a held customer's, a held order, an open one) and the confirmation emails; holds refused with the wrong subject, kind, class, no reason or by a cashier, placed on a customer, an order and a whole class, a rival neither releasing nor seeing them; the order purge refused with no period, twenty sweeps at once redacting the one order due once between them and each counting the two held, the open order kept, a cashier, storekeeper and shopper refused, a rival's owner purging only their own business; the log purge stopped by the class hold, the release needing a reason and refused to a cashier and a second time, then the messages gone and the held customer's kept; the inactive customer erased and the held one kept, a second run finding nothing; every run on the register from all three services, a rival's empty, a storekeeper refused, the transactions never purged | 75 |
| `web-shell` | The web app's own responses (12.11, SJ-D60, SJ-D61; `WEB_URL`, default http://localhost:8088): the entry page, `index.html`, an SPA route, the bundles, a 304, a missing file, the manifest, a HEAD, a POST and a route with markup in it each carry the Content-Security-Policy (scripts from this origin and WebAssembly only, no inline script or eval, no plugins, no base rewrite, no framing by another site, the gateway origin in connect-src), nosniff, same-origin framing, a referrer policy and exactly one Cache-Control; the entry page is never stored and the bundles are revalidated, a returning browser getting a 304; the page declares its language, announces its loading message and carries no inline script; CanvasKit is served from the origin; every icon the page and the manifest name is served as a PNG; a missing bundle is a 404, a POST a 405, a traversal and reflected markup reach nothing; the API proxy, security.txt and the health probe still answer | 135 |
| `ledger-flow` | The accounting seam (17.1, 17.3, 04.7, 07.7): a goods receipt posts the stock against the accrual on the store's mapped nominal code; a supplier invoice posts the creditor with a due date and is checked against its own stated total; a flagged invoice is approved or rejected by a manager, a rejection reversed line for line and taken out of the VAT return; a credit note reverses the creditor; finance posts a manual journal and reads the trial balance; a closed period refuses a posting — with the wrong roles, the wrong tenant, journals that do not balance and twenty rejections at once | 72 |
| `sales-posting` | Sales and tender posting to revenue and control accounts (17.7): a till sale paid part in cash and part by card reaches the ledger as tender postings against receipts clearing and a sale posting that clears it into sales and VAT output; a card refund takes revenue and VAT back and credits card clearing; money taken for a sale never confirmed stays on the clearing report until refunded — with the wrong roles, a rival tenant, a bad store, ten payments on one key | 23 |
| `payment-run-flow` | Supplier payment runs and remittance (17.10): suppliers carry bank details and a remittance email; a manager proposes a run from what is due, offsetting a supplier's credit note and leaving out the supplier with no bank details; the owner approves it; the bank file is downloaded; paying the run settles the invoices and posts Dr Creditors / Cr Bank; the remittance advice reaches notification-svc — with the wrong roles, the wrong tenant, the proposer approving their own run, a storekeeper setting bank details, bank details changed after approval, twenty payments at once and ten proposals racing for the same invoices | 47 |
| `gross-margin` | Gross margin and GMROI (19.7): a till sale's revenue, net of VAT, reaches inventory-svc with the stock it drew and is set against the cost of the batches it drew down — the same revenue the ledger posted to sales for that order; GMROI is the margin over the average holding stock turn reports; a return takes back its share of revenue and cost; a voided sale earns nothing; by store, a sale rolls up to its store — with the wrong roles, a rival tenant, a forged tenant header, a window that ends before it starts, a bare date, a grouping that is not one, SQL in the grouping, an absurd limit clamped and twenty concurrent reads answering one figure | 29 |
| `stock-provenance-flow` | Stock that moves keeps what it is (SJ-D71, 05.1): two lots received with dates and costs, a direct transfer arrives as one batch per lot drawn — soonest date first — with each lot's date and cost, the sender keeps the rest, the receiving store's expiring view sees it, the genealogy ties each arrival to its source, and a recall on the lot holds the moved stock IN_SCOPE rather than LOT_UNKNOWN; an in-transit transfer arrives as what left, read back from the ledger; a move within a store puts the picked lot down under its own number; stock with no lot moves under TO- and still keeps its date; a till sale and a return, and the return comes back under the lot it was sold from, on the expiring view again — with a transfer short of stock shipping and delivering nothing, a cashier refused, a move not picked twice, a direct transfer not received twice, and a rival seeing no batch and no lineage | 31 |
| `fx-flow` | Multi-currency pricing and FX (03.x) through the gateway: a business's exchange rates — home currency only to begin with, a dollar rate set with a reason and read by a storekeeper (the read the services make), a second rate and the history newest first; refused by name for the home currency, an unknown code, a rate of nothing, a day before 2000 or not a date, no reason, and by role; the currencies a shop can show; a price and a basket shown in dollars beside the sterling charged, the euro refused for want of a rate; a dollar purchase order keeping the rate and the sterling figure its authority was measured against. | 21 |
| `price-zones-flow` | Price zones and competitor-driven repricing (03.x) through the gateway: a zone with the north store in it and a price list bound to it, so the north till charges the zone price while the south store and a store-less lookup get the tenant-wide one; refused by name for a duplicate zone, another business's store, a zone nobody made, a list on one, and a cashier drawing zones; another business sees none of it; rivals recorded one at a time and in bulk in the business's own currency (another currency, a future day and a cashier refused); a rule on the zone's list (its zone the list's; a strategy nobody knows, an undercut over 100% and a list nobody made refused) run to one proposal — 9.00 → 7.99 against Rival A at 8.50, a stale January sighting ignored — refreshed not duplicated by a second run, applied so the north store charges 7.99 and the south still 10.00, decided once, nothing left to propose afterwards, the applied one on the record, a cashier reading no proposals. | 33 |
| `consignment-flow` | Consignment stock ownership through the gateway: a purchase order on consignment (an ownership nobody defined refused), received with nothing posted to the ledger and not invoiceable on receipt; the batch inventory-svc makes from the GoodsReceived event is the supplier's at the order's price and the valuation sets it apart (5 on consignment worth 15.00, none of it our value); consignment stock with no supplier and an unknown ownership refused at goods-in; a paid till sale of two fulfilled, heard by purchase-svc as one sale owed at 3.00 with the ledger owing the supplier 6.00 the moment it sold and 3 still the supplier's on the shelf; a settlement statement of 6.00 for one sale with a CS- reference, nothing left unsettled, the statement reading back with its sale, the same period again refused, a period ending before it starts refused, a cashier neither settling nor reading. | 22 |
| `loyalty-flow` | Loyalty tiers and points expiry (13.x) through the gateway: the platform's default programme, the business's own set by management (three tiers with multipliers, points living twelve months, a twelve-month qualifying window) and refused by name when out of order, without months, or ill-named; a customer reaching SILVER on twelve qualifying points with the next tier thirty-eight away; a till sale earning at SILVER's ×1.5 with the ledger saying so; spending that leaves the tier where it was; the sweep run now finding nothing due; the rule lifted so points live for ever; a cashier and a storekeeper refused; no token. Points dying is proved in `LoyaltyProgrammeIT` over back-dated lots. | 20 |
| `deferred-revenue` | Deferred revenue for loyalty points and gift card breakage (17.11): a customer's points on a till sale wait until a point's value and the breakage estimates are set, then post the points' share of the sale's net revenue from sales (4010) to deferred income (2330); spending half releases its share to 4020, the lapse of the rest is breakage (4030); a gift card sold by card posts card clearing against the liability (2310) and one given away a cost (6420); a gift card tender recognises breakage (4031) once, a replay on its key adding nothing; the trial balance balances and a change of estimate keeps the earlier one — refused: a cashier or storekeeper, a point worth 0 or over 1000, 96% or -1% breakage, no reason, a point value that is not a number, a gift card with no paid-by or paid with a gift card or store credit, more points spent than held; a rival tenant sees nothing | 34 |
| `bank-files` | Bank-standard payment files (17.12): the business sets the accounts it pays from, one per currency, masked on the way back; an approved sterling run downloads as a Bacs Standard 18 file (VOL1 to UTL1, 80-character labels and 100-character records, the credit and its contra) and a euro run as a SEPA pain.001 from the euro account to each IBAN, the same file each time; the bank's pain.002 is uploaded back — a close match on a payee's name is held, the run is not paid until a manager releases it with a reason, then it is; a payee the bank could not match cannot be released and a rejected file holds every payment — refused: a storekeeper or cashier, a bad sort code, IBAN, service user number or currency, a euro account without an IBAN, the wrong format for the currency, an unknown format, a DTD in the report, a report for another file or naming a payment the run does not make, a paid run, a payment the bank did not hold; abuse: ten releases at once release once, a replayed report does not undo the release, the paying account changed after approval stops the Bacs file, a Bacs file cannot pay today, a supplier with no IBAN is named; a rival tenant sees nothing (euro orders need EUR rows in `PURCHASE_APPROVAL_LIMITS` when it is set) | 52 |
| `tenant-export` | Per-tenant export, erasure and import (21.14, EU Data Act ch.VI): a business with a store, a catalogue item, a manager and a supplier takes out what all twelve services hold for it, manifest and pages, the rows matching every manifest's count and no credential among them, with the register of what is left out and what is kept at erasure; the owner gives notice to erase today, the platform's sweep starts the erasure, every service erases its share and the notice reads ERASED with evidence from all twelve; a fresh business imports the export and every table it did not already have reads back row for row and checksum for checksum — refused: a manager reading the export or giving notice, a rival, a table left out, a cursor the pages did not give, notice longer than two months, a second notice, an owner running the platform's sweep, a notice or the business record imported, rows that are not objects; abuse: five sweeps at once, the erased owner signing in or refreshing again, a token issued before erasure reading only an empty business, the same page imported again, ten imports of it at once | 30 |
| `einvoice-inbound` | Supplier e-invoices in (07.13): the owner records the business's VAT number and e-invoicing address; a supplier's UBL invoice naming its order and order line is captured through the three-way match (MATCHED, 25.00 net) and downloads byte for byte, named and not sniffed; a Factur-X PDF crosses the gateway intact, is read from the CII inside it and waits for its order until a person picks it, and the item code that person matched captures the supplier's next invoice on its own; an invoice from a sender nobody knows waits for its supplier, is captured once named, and its next invoice is known by the address it came from; a credit note closes the return it credits — refused: a VAT number or GLN that does not parse, half an address on the business or a supplier, a storekeeper setting them or refusing, an order that is not the supplier's, a line from another order, not on the invoice or chosen twice, a refusal without a reason, a settled e-invoice refused or matched again, a non-compliant or misdirected invoice matched, a status that does not exist; abuse: the same bytes twice, the same number in another file, a storekeeper's credit note left for a manager, a second credit note, an address another supplier holds, a DTD, XML that is not an invoice, a file claiming to be a PDF, an empty body, JSON, a card number inside an invoice, 1.5 MB to another route, more than 10 MB of XML, more than 21 MB at all, another tenant's reads, lists and matches, ten matches at once capturing once. Fetching (07.13's Polish half): a business that never chose where to fetch from is told what to do and no ministry is asked; the platform can stand in for KSeF and no token is asked for, and the readiness check refuses to call that ready (nothing leaves the platform, so nothing arrives), as a fetch does rather than answering as an empty success — refused: a network or provider that does not exist, an account that is not a NIP, the ministry on a deployment that holds no endpoint for it, a storekeeper choosing or asking; abuse: a rival seeing its own inbox. Fixtures in `k6/fixtures/` are written by the shared einvoice module, so they break no rule | 84 |
| `einvoice-outbound` | Invoices to business buyers (18.9): the owner records the business's VAT number and Peppol address, and a customer as a VAT-registered business with a billing address; a till sale to that customer, paid in cash, is invoiced without anyone asking (INV/yyyy/nnnnnn, net 20.00 / VAT 4.00 / 24.00, Peppol BIS Billing 3.0 as both have an address), asking again returns the same document, and it downloads as the UBL issued, as CII and as a Factur-X PDF, each named by its number, not sniffed, not cached; a till discount off the total is stated net (21.00 → 17.50 + 3.50); a return is credited without asking (CRN, typed 381, naming the invoice) and once; the register pages newest first and the invoice numbers run 1–4 with no gap past every refusal; an Indian business's sale is invoiced at 18% GST and written as the IRP's INV-01 (schema 1.1, IGST across states, the HSN code), and an item with no HSN code keeps the portal's objection and refuses the IRP download — refused: a basket never paid for, a sale naming nobody, a customer not registered or without an address, IRP for a British business, an unknown format, an unknown sale, document or return, a return that was never invoiced, a cursor that is not a document; abuse: a cashier issuing, a storekeeper reading, a cashier downloading, another tenant's reads, downloads and requests, a path that is not an id, ten requests at once answering with one document | 58 |
| `einvoice-transport` | The e-invoicing transport seam over the simulated network: the settings say what this deployment offers (four networks, SIMULATED on each, the Peppol access point, France's platform, India's portal and Poland's KSeF deployed but not choosable without credentials, the portal and KSeF taking the business's own credential, the business's own address, what its country asks for); the owner chooses Peppol; a till sale to a registered buyer is invoiced and delivered without anyone asking, with the network's reference and the buyer's address kept; a buyer the network does not know is refused for good and refused again when sent by hand, both attempts kept; a slow receiver is taken, asked after and delivered; a buyer with no electronic address has nowhere to receive; the outbox lists, filters and pages every attempt — refused: a network or provider that does not exist, the access point, the platform, the portal and KSeF without credentials, a credential on a deployment with no secrets key, Peppol for a business with no address, a provider not named, an account too long, sending what was delivered, sending with no network chosen, a status or cursor that does not exist, a cashier choosing or reading; abuse: another tenant's outbox, attempts and sends, a path that is not an id, ten sends at once for one document never overlapping (each 200 one attempt, each other told). Readiness: a business that never chose is told what stands in the way and no network is asked anything, and a business that never recorded a VAT number is told to record one rather than told its details could not be read; with Peppol chosen the network is tried with what the business holds and answers READY, and ready says what it means on a stack with no contract (nothing leaves the platform, a provider contract sends them further); the outbox is where it was, so a check sent nothing; with the network given up again it is not ready and asks nobody — refused: a cashier asking; abuse: a rival seeing its own readiness and never this business's network. Delivery in: a sale to a business on this platform is delivered into that business's inbox, where it waits for its supplier by the network it came over with the network's reference, byte for byte the document issued, and the sender's own inbox has nothing of it; the delivery route as an access point calls it: the same document again is already there (by the first receipt's id, learning nothing of the receiver's own) — refused: no key, a wrong key, KSeF (pulled, not delivered), a network that does not exist, a buyer nobody on this platform holds, no document, JSON, XML that is not an invoice; abuse: a staff token as a key, a rival's token with a tenant header, ten deliveries at once of one document making one inbox row | 81 |
| `gateway-login-protection` | Five failed logins lock the account and the IP (429 `LOGIN_LOCKED`, `Retry-After`) | 13 |
| `gateway-unsubscribe-protection` | Five guessed opt-out tokens lock the IP out of the public unsubscribe endpoint (429 `TOKEN_LOCKED`); a malformed body is not a guess; the customer stays subscribed; the rest of the API is unaffected | 11 |
| `gateway-card-data-guard` | The PCI scope as a control: a card number in a description, a barcode field, a nested PUT, a query string, an order note or an unauthenticated login is refused at the gateway (400 `CARD_DATA_NOT_ACCEPTED`) and never echoed; UK and German EAN-13s, a GTIN-14, an IMEI, a phone number, an all-digit id and a Luhn-failing sixteen digits pass; a till sale is placed and paid with the guard in place; twenty-five in a row lock nothing | 22 |
| `purchase-crud` | A supplier corrected after creation (SJ-D34): terms and VAT number under an open order, the currency only once the order is cancelled, the cancelled order keeping its currency and the next inheriting the fix; a duplicate name, a bad currency, zero-day terms and a blank name refused; a cashier and a rival tenant refused; then return to vendor (07.8): refused before anything arrived, ten cases received and booked by inventory-svc, an invented reason, an empty return, more than received, a cashier and a rival refused, three cases sent back by the storekeeper with a numbered debit note at the order's price, the three leaving the shelf within 30 s, the order's progress reading received 10 / returned 3 with its status untouched, eight of the seven left refused, the returns read by any staff member and not by a rival, the credit note refused to a storekeeper and for a bad date, recorded by the owner at the debit note's gross, refused a second time, nothing deleting it | 46 |
| `vat-return-flow` | SJ-D39 closed: a supplier invoice captured in purchase-svc reaches the VAT return as box 4 (input VAT) and box 7 (net purchases) within seconds, box 5 is what is owed, a rival tenant's return is untouched, a cashier is refused, a backwards period is refused; then Making Tax Digital (18.5): the offer read before registering, a wrong check digit and an unknown provider refused, HMRC refused unless configured, the number registered through the simulator, the obligations read, a return not declared final and a bad period key refused, a cashier refused, the quarter filed from the records with box 4 = 30 and box 7 = 150 whole pounds and HMRC's receipt kept, the second filing refused, the obligation fulfilled, the filing listed and readable by id but not by a rival, nothing deleting it | 38 |
| `tenant-defaults` | Tenant defaults (SJ-D53): a business that trades in yen gets yen, and one in dinars gets dinars, wherever a request leaves the currency or country out — a price list, a customer's VAT status, a supplier, store credit, a Z-report, a gift card, a till sale — with contradicting and malformed codes refused and twenty defaulted writes at once agreeing; then no assumed VAT rate (SJ-D56) — a French business with no standard rate refused at the shopper's quote, the basket and the till sale with `PRICING_VAT_RATE_NOT_CONFIGURED`, a cashier refused setting the rate, a percentage sent as the fraction refused, twenty shoppers at once refused by name and twenty owners at once making one rate, then 20% quoted and the sale placed; and a garbled or mistyped body refused `400 REQUEST_BODY_INVALID` on the pricing, order, product, customer and purchase services rather than a 500 (SJ-D57) | 35 |
| `full-stack-simulation` *(load)* | One business browsing, reserving, selling at the till and restocking concurrently | ~1100 |
| `multi-tenant-retail` *(load)* | Two tenants (IN/INR, UK/GBP) across 28 concurrent scenarios; gates on zero isolation and security violations and a low error count | ~24000 |
| `gateway-rate-limit-stress` *(load)* | Exactly the configured per-IP budget is admitted in a 60 s window, then 429 `RATE_LIMITED`, never 5xx | ~30600 |

Every functional suite requires **all** checks to pass (`thresholds: { checks: ['rate==1.0'] }`).

Things to know:

- **Gateway suites lock this host out.** `gateway-login-protection` blocks logins and `gateway-unsubscribe-protection` blocks the opt-out endpoint from the k6
  machine for 15 minutes and `gateway-rate-limit-stress` spends its whole rate budget. `run.sh`
  clears those counters in the local `storeql-redis` afterwards; run them directly with `k6 run`, or
  against a remote stack, and logins from this host stay blocked.
- **Status changes are asynchronous.** Suspensions and closures travel as `TenantStatusChanged` /
  `StoreStatusChanged` through the outbox and Kafka, and the gateway caches tenant status for 15 s.
  `flow-guard-runtime` polls each guard for up to 60 s and prints how long each took.
- **`multi-tenant-retail` shares fixtures between VUs.** Scenarios pick their tenant by VU parity
  and their variant by iteration, so two VUs occasionally act on the same batch or setting at once
  and a handful of read-back checks fail per run. Its thresholds gate on errors, isolation and
  security violations rather than on every check.
- **Purchase approval.** With `PURCHASE_APPROVAL_LIMITS` set, a currency without limits fails closed
  and nobody can approve its orders. The retail tenants trade in INR and GBP, so both need limits.

## Writing a suite

Put the file in `k6/` as `<area>-crud.js` (or `flow-guard-*.js`), add its name to `FUNCTIONAL` or
`LOAD` in `run.sh`, and build it from `lib/storeql.js`:

```js
import { ALL_CHECKS_PASS, call, data, expect, onboardTenant, register, truthy } from './lib/storeql.js';

export const options = { vus: 1, iterations: 1, thresholds: ALL_CHECKS_PASS, setupTimeout: '3m' };

export function setup() {
  // setup() runs once; what it returns is passed to the default function.
  return { tenant: onboardTenant('widgets', { stores: 1 }), rival: onboardTenant('widgets-rival'), shopper: register('widgets-shopper') };
}

export default function ({ tenant, rival, shopper }) {
  const t = tenant.owner.token;
  const made = call('POST', '/api/product-svc/admin/brands', { token: t, body: { name: 'Acme' } });
  expect(made, '[+] create brand', 201);
  expect(call('POST', '/api/product-svc/admin/brands', { token: t, body: {} }), '[-] name required', 400, 'VALIDATION_FAILED');
  expect(call('GET', `/api/product-svc/admin/brands/${data(made).id}`, { token: rival.owner.token }), "[-] a rival cannot read it", 404);
  expect(call('POST', '/api/product-svc/admin/brands', { token: shopper.token, body: { name: 'x' } }), '[-] a customer cannot', 403);
  expect(call('POST', '/api/product-svc/admin/brands', { body: { name: 'x' } }), '[-] no token', 401);
}
```

`lib/storeql.js` provides:

| Helper | Does |
|---|---|
| `call(method, path, { body, token, storefront, idem, headers })` | One request through the gateway. `storefront: tenantId` sets `X-Storefront-Tenant` for guest and shopper paths; `idem: true` sends a fresh `Idempotency-Key` (required to place an order), or pass the key to replay one. Ids in the path are folded out of the metric name. |
| `data(res)`, `errorCode(res)`, `nextCursor(res)` | Read the `{ data, error, meta }` envelope. |
| `expect(res, label, status, code?)` | A check on the status (one or a list) and, optionally, the error code. Logs the response when it fails, so a red check says why. |
| `truthy(label, value, detail?)` | A check on a computed value; logs `detail` when it fails. |
| `must(res, status, what)` | For prerequisites: throws (failing setup or the iteration) unless the status matches. |
| `poll(seconds, fn)` | Retries `fn` once a second until it returns truthy; returns the seconds taken or -1. Use it for anything that crosses Kafka. |
| `register(label)`, `login(user)`, `signInUntil(user, claimsPredicate)` | Identities. A sign-up is a CUSTOMER; `signInUntil` logs in until the token carries what an event granted (tenant, role). |
| `onboardTenant(label, { country, currency, stores })` | Owner, tenant and stores, with the owner's token already carrying the tenant and OWNER. |
| `staffUser(tenant, role, storeIds)` | A staff login assigned at those stores, signed in with the role. |
| `platformAdmin()` | The bootstrap platform admin (env `PLATFORM_ADMIN_EMAIL` / `PLATFORM_ADMIN_PASSWORD`). |
| `sellableVariant(tenant, name)`, `priceVariants(tenant, variantIds, price)`, `receive(tenant, storeId, variantId, qty)` | A sellable product with one variant, an active price list in the tenant's currency, a stock receipt. |
| `setTenantStatus(admin, tenantId, status)`, `setStoreStatus(tenant, storeId, status)` | The flow-guard switches. |
| `newId()`, `newKey(prefix)`, `uniq()` | A UUIDv7 for any id a script sends that gets stored (StoreQL keeps only v7 — `validate_all.sh` fails on anything else), an idempotency key, a run-unique suffix. |

Conventions the existing suites follow:

- **Positive and negative.** Label checks `[+]` and `[-]`. For each capability prove the happy
  path, the validation failure (400 with the code), the wrong role (403), no token (401) and another
  tenant (404, never 403 — a 403 would confirm the id exists).
- **Exact expectations.** Pin the status and the error code the service documents. A check that
  accepts "any 4xx" or "200 or 404" passes whatever happens. When a check fails, find out whether
  the service or the expectation is wrong before changing either.
- **Isolation.** Use a second tenant from `onboardTenant` for cross-tenant checks, never a made-up
  id: a random id is not found for everyone and proves nothing.
- **Asynchrony.** Anything fed by events (role grants, status projections, reports, alerts) is
  polled with `poll` or `signInUntil`, never read once after a `sleep`.
- **Run data is disposable.** Every suite creates its own tenants with unique names, so suites can
  run in any order and repeatedly against the same database.

## Flow guard: adding a guarded surface

A new place where money or stock moves must be added to both flow-guard suites:

1. The service checks `TenantStatusRepository.isActive(tenantId)` and
   `StoreStatusRepository.isActive(tenantId, storeId)` (common-service projections fed by
   `BaseTenantStatusChangedConsumer` / `BaseStoreStatusChangedConsumer`) and answers 409
   `TENANT_NOT_OPERATIONAL` / `STORE_NOT_OPERATIONAL`.
2. In `flow-guard-runtime.js`, add a probe function for it and call it in each phase: open at
   baseline, refused within `PROPAGATION` after suspension and after closure, open for the other
   tenant and store throughout, open again after reopening.
3. In `flow-guard-comprehensive.js`, add the step where it belongs in onboarding-to-sale, with its
   too-early, wrong-role and other-tenant refusals.
