# Base configuration for sample-svc (all profiles). Non-secret values only.
# Served by config-svc at: GET /config/sample-svc/<profile>
shelfj.service.name=sample-svc
server.port=8000
shelfj.consul.enabled=true
shelfj.consul.host=localhost
shelfj.consul.port=8500
