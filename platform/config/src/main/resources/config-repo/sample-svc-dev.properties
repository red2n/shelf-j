# dev-profile overlay for sample-svc (merged on top of sample-svc.properties).
# DB pointers for local dev. Secrets still come from env/secret store, not here.
shelfj.db.url=jdbc:postgresql://localhost:5432/shelfj
shelfj.db.user=shelfj
